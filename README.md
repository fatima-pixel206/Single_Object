# Single_Object
A class have only one object
<br>
the static class has more than one object<br>
but if the object is limited in that class you want to create an object then there should be only one onj
